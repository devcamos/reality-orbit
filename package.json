{
  "name": "reality-orbit",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive orbit map for exploring dimensions of reality.",
  "scripts": {
    "check": "node scripts/verify.mjs && node --test scripts/*.test.mjs",
    "test": "node --test scripts/*.test.mjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
